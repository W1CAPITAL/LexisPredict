/**
 * @fileOverview Provider Especializado para Tribunais de Justiça (Estaduais)
 */

import { CNJProvider } from './CNJProvider';

export class TJProvider extends CNJProvider {
  // Implementação específica para TJs se necessário no futuro
  // Por enquanto herda do CNJProvider (DataJud)
}
