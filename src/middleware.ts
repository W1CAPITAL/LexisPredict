/**
 * NÃO use este arquivo. O Next só deve ter UM middleware.
 * Sessão + headers + cargos: /middleware.ts (raiz).
 *
 * Mantido só como reexport para não quebrar imports antigos.
 */
export { middleware, config } from "../middleware";
